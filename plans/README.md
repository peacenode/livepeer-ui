# Implementation Plans

Generated by the improve skill on 2026-07-24. Execute in the order below unless
dependencies say otherwise. Each executor must read the selected plan fully,
honor its STOP conditions, and update its row when done.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| [001](./001-schema-driven-playbook-forms.md) | Replace heuristic playbook forms with a schema-driven renderer | P1 | L | — | TODO |

Status values: TODO, IN PROGRESS, DONE, BLOCKED (with a one-line reason), or
REJECTED (with a one-line rationale).

## Dependency notes

- Plan 001 is self-contained and deliberately uses a compatibility adapter
  because the Storyboard backend is not part of this repository.
- A future structured-catalog plan should follow Plan 001, not block it. Form
  correctness and path-aware serialization are the higher-risk dependency.

## Findings considered and rejected

- **Rewrite every playbook into a frontend-owned schema immediately**:
  rejected because it would create a second source of truth for 63 editable
  documents and drift from execution behavior.
- **Continue adding playbook-specific conditions inside the React component**:
  rejected because the current brand exception already demonstrates the
  maintenance and regression cost.
- **Make tests fetch the live catalog**: rejected because network-dependent
  fixtures would be slow, flaky, and capable of changing without a code review.
- **Include catalog HTML scraping in Plan 001**: deferred. It should move to a
  structured endpoint, but it is separable from form correctness and would
  enlarge the migration blast radius.
